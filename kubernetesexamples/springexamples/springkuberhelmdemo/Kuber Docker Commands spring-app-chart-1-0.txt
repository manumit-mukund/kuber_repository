cd C:\mygitrepositories\kuber_repository\kubernetesexamples\springexamples\springkuberhelmdemo

docker build -t spring-helm-app:1.0 .

docker login -u manumitmukund -p Mkdmkd`123

docker tag spring-helm-app:1.0 manumitmukund/mykuberrepository:spring-helm-app_1_0

docker push manumitmukund/mykuberrepository:spring-helm-app_1_0

helm create spring-helm-app-1-0-chart-1-0

--Edit values.xml as shown below in spring-helm-app_1_0-chart-1-0 

image:
  repository: manumitmukund/mykuberrepository
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "spring-helm-app_1_0"

  service:
  type: NodePort
  port: 8081

--Edit values.xml accordingly in spring-helm-app_1_0-chart-1-0 


tree spring-helm-app-1-0-chart-1-0

helm install myhelmapp-chart-1-0 spring-helm-app-1-0-chart-1-0

Verify the helm release: helm list -a

if required: helm uninstall myhelmapp-chart-1-0

kubectl port-forward svc/myhelmapp-chart-1-0-spring-helm-app-1-0-chart-1-0 8081:8081

hit the url (for the index page): http://localhost:8081/

hit the url: http://localhost:8081/kuber/hello

Npte: If the above hits don't work, delete minikube and start it again, then follow the above commands from helm install.
