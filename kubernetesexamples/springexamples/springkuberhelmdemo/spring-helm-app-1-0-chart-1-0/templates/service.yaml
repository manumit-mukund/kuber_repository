apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-helm-app-1-0-chart-1-0.fullname" . }}
  labels:
    {{- include "spring-helm-app-1-0-chart-1-0.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-helm-app-1-0-chart-1-0.selectorLabels" . | nindent 4 }}
