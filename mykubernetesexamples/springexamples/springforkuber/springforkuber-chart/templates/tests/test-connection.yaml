apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "springforkuber-chart.fullname" . }}-test-connection"
  labels:
    {{- include "springforkuber-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "springforkuber-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
